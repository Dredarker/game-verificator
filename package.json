{
  "name": "game-verificator",
  "version": "1.0.0",
  "private": true,
  "description": "Simple HTTP server that echoes GET query parameters.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  }
}